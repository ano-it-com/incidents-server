{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}
    {% if app.user %}
        You are logged in as {{ app.user.username }}, <a href="{{ path('app_logout') }}">Logout</a>
    {% else %}
        Please, <a href="{{ path('app_login') }}">Login</a>
    {% endif %}
{% endblock %}
